extends layout

block content
    .mui-container-fluid
        .mui-row
            .mui-col-xs-12
                section
                    h1 #{message}
                    h2 #{error.status}
                    pre #{error.stack}
